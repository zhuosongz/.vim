priority -40

# We use a little hack so that the snippet is expanded
# and parsed correctly
snippet msnip "Ultisnips snippet definition" b
context "math()" 
`!p snip.rv = "snippet"` ${1:Tab_trigger} "${2:Description}" ${3:b}
${0:${VISUAL}}
`!p snip.rv = "endsnippet"`
endsnippet


# vim:ft=snippets:
